;; Node.js toolchain (NVM + npm)
;; Provides: node, npm, npx via NVM; npm cache writes

(allow process-exec
  (subpath (string-append (param "HOME") "/.nvm")))

(allow file-read-data
  (subpath (string-append (param "HOME") "/.nvm"))
  (subpath (string-append (param "HOME") "/.npm")))

(allow file-write*
  (subpath (string-append (param "HOME") "/.npm")))
